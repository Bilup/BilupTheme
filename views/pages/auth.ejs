<!DOCTYPE html>
<html lang="<%= Lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - BilupTheme</title>
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <script src="/static/js/lucide.js"></script>

    <link rel="stylesheet" href="/static/css/styles.css">
</head>
<body>
    <main class="auth-container">
        <div class="auth-card">
            <div class="auth-header">
                <i data-lucide="palette" class="auth-logo"></i>
                <h1><%= t('auth.welcome') %></h1>
                <p><%= t('auth.subtitle') %></p>
            </div>
            
            <div class="auth-providers">
                <div class="auth-providers-header"><p><%= t('auth.loginWith') %></p></div>
                <div class="auth-options">

                    <a id="rotur-btn" href="https://accounts.bilup.org/auth?return_to=<%= SiteOrigin %>/auth" class="auth-btn rotur">
                        <div class="auth-btn-logo"><img src="https://accounts.bilup.org/favicon.ico" alt="Rotur"></div>
                        <div class="auth-btn-content">
                            <strong>Bilup Accounts</strong>
                        </div>
                        <div class="auth-btn-arrow"><i data-lucide="arrow-right"></i></div>
                    </a>

                </div>
            </div>
        </div>
    </main>

    <%- include('../partials/footer', { ...locals }) %>
    <script src="/static/js/auth.js"></script>
    <script>lucide.createIcons();</script>
</body>
</html>
