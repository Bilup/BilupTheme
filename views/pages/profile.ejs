<!DOCTYPE html>
<html lang="<%= Lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= ProfileUser.username %>'s Profile - BilupTheme</title>
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
    <script src="/static/js/lucide.js"></script>

    <link rel="stylesheet" href="/static/css/styles.css">
</head>
<body>
    <%- include('../partials/nav', { ...locals }) %>

    <main class="main-content">
        <div class="profile-header">
            <div class="profile-avatar">
                <% if (AuthType === 'scratch') { %>
                    <% if (ProfileUser.avatar) { %>
                    <img src="<%= ProfileUser.avatar %>" alt="<%= ProfileUser.username %>">
                    <% } else { %>
                    <img src="<%= h.getScratchUserAvatarURL(ProfileUser.username) %>" alt="<%= ProfileUser.username %>">
                    <% } %>
                <% } else if (AuthType === 'rotur') { %>
                <img src="https://avatars.accounts.bilup.org/<%= ProfileUser.username %>" alt="<%= ProfileUser.username %>">
                <% } else if (AuthType === 'github' && ProfileUser.avatar) { %>
                <img src="<%= ProfileUser.avatar %>" alt="<%= ProfileUser.username %>">
                <% } %>
            </div>
            <div class="profile-info">
                <h1 class="profile-name"><%= h.formatUsername(ProfileUser.username, AuthType) %></h1>
                <p class="profile-meta">
                    <span><i data-lucide="palette"></i> <%= Themes ? Themes.length : 0 %> <%= t('profile.themes') %></span>
                    <span><i data-lucide="calendar"></i> <%= h.formatDate(ProfileUser.createdAt, Lang) %> <%= t('profile.joined') %></span>
                </p>
            </div>
        </div>

        <div class="profile-themes">
            <h2><%= t('profile.themes') %></h2>
            <% if (Themes && Themes.length > 0) { %>
                <div class="themes-grid">
                    <% Themes.forEach(function(theme) { %>
                    <%- include('../partials/theme-card', { Theme: theme, Mods: Mods, ShowAuthor: false, ShowActions: false, ShowDate: false, Users: {} }) %>
                    <% }); %>
                </div>
            <% } else if (IsOwnProfile) { %>
                <%- include('../partials/empty-state', { icon: 'palette', message: t('myThemes.noThemes'), cta: true, ctaUrl: '/upload', ctaText: t('upload.uploadBtn') }) %>
            <% } else { %>
                <%- include('../partials/empty-state', { icon: 'palette', message: t('myThemes.noThemes'), cta: false, ctaUrl: '', ctaText: '' }) %>
            <% } %>
        </div>
    </main>

    <%- include('../partials/footer', { ...locals }) %>
    <script src="/static/js/main.js"></script>
    <script src="/static/js/profile.js"></script>
    <script>lucide.createIcons();</script>
</body>
</html>
